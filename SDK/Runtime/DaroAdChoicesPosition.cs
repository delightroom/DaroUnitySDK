#nullable enable

namespace Daro
{
    /// <summary>Physical corner of the full native ad area, independent of UI language.</summary>
    public enum DaroAdChoicesPosition
    {
        TopLeft = 0,
        TopRight = 1,
        BottomRight = 2,
        BottomLeft = 3,
    }
}
